apiVersion: apps/v1
kind: Deployment
metadata:
  name: webfrontend-{{.Release.Name}}
spec:
  replicas: 1
  selector:
      app: webfrontend-{{.Release.Name}}
  template:
    metadata:
      labels:
        app: webfrontend-{{.Release.Name}}
    spec:
      containers:
      - name: webfrontend-{{.Release.Name}}
        image: blackbirddev.azurecr.io/webfrontend:v1
        ports:
        - containerPort: 80
        env:
        - name: TITLE
          value: {{ .Values.title }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.serviceName }}
spec:
  type: {{ .Values.serviceType }}
  ports:
  - port: 80
  selector:
    app: webfrontend-{{.Release.Name}}
